/// [ElTooltipPosition] is used to define the desired position of the tooltip
enum ElTooltipPosition {
  topStart,
  topCenter,
  topEnd,
  rightCenter,
  bottomStart,
  bottomCenter,
  bottomEnd,
  leftCenter,
}
